{
  "name": "@intelligence/domain-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Domain SDK contracts for IntelligenceOS",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
 "scripts": {
  "build": "tsc -p tsconfig.json",
  "build:edge": "pnpm run build && pnpm exec esbuild src/index.ts --bundle --platform=neutral --format=esm --packages=external --outfile=edge/index.js",
  "clean": "rm -rf dist edge"
},


  "dependencies": {
    "@intelligence/contracts": "workspace:*"
  },
  "devDependencies": {
  "esbuild": "^0.28.1",
  "typescript": "^5.9.2"
}
}