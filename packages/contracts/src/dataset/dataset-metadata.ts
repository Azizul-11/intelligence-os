/**
 * Metadata associated with a dataset.
 */
export interface DatasetMetadata {
  /**
   * Dataset description.
   */
  description?: string;

  /**
   * Dataset tags.
   */
  tags?: string[];
}