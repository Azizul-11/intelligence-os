/**
 * Describes the structural type of a metric.
 */
export enum MetricKind {
  NUMBER = "number",
  PERCENTAGE = "percentage",
  CURRENCY = "currency",
  COUNT = "count",
  DURATION = "duration",
  BOOLEAN = "boolean",
  TEXT = "text",
  SCORE = "score",
}